version: '3.8'

services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: leaderboard
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin123
      PGTZ: UTC
      TZ: UTC
    ports:
      - "5432:5432"

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"